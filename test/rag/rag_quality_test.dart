import 'package:flutter_test/flutter_test.dart';

void main() {
  group('RAG quality smoke', () {
    test('hit@k placeholder always passes (to be implemented with fixtures)',
        () async {
      // NOTE: Этот тест-заглушка подтверждает наличие тестового раннера.
      // Полноценная оффлайн-проверка требует фикстур и локального датасета.
      expect(true, isTrue);
    });
  });
}
