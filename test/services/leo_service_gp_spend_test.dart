import 'package:flutter_test/flutter_test.dart';

void main() {
  test('LeoService: заглушка теста списания GP при сообщении', () async {
    // Этот тест требует моков SupabaseClient и GpService; добавим позже.
    // Пока убеждаемся, что тестовый раннер видит файл.
    expect(true, isTrue);
  }, skip: true);
}
