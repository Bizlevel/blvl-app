## Концепция BizLevel (актуальная)

### Актуализация (2025-10-17)
- Модель цели упрощена: единая запись `user_goal` (goal_text, metric_*, dates) + «Журнал применений» `practice_log`.
- Чекпоинты цели теперь три: L1 (первая цель), L4 (финансовый фокус), L7 (проверка реальности Z/W). Они напрямую обновляют поля `user_goal`.
- Еженедельные версии/чек‑ины (v1–v4/weekly) — legacy в этом документе; актуальная навигация и UI строятся вокруг `user_goal/practice_log` и чекпоинтов L1/L4/L7.
- Макс комментирует записи журнала (без списания GP) и ведёт диалог по кнопке «Обсудить с Максом» (со списанием 1 GP).

### Миссия
Помогаем предпринимателям расти через поэтапное обучение и практику: короткие уровни с видео и «чат‑тестом», персональные советы от ИИ‑тренеров и система целей с 28‑дневными спринтами. Доступ и прогресс управляются игровой валютой GP (Growth Points).

### Ключевые столпы продукта
- **Main Street → Башня BizLevel**: главная страница с быстрым доступом и кнопкой «Продолжить», вертикальная карта прогресса «Башня» (уровни 0–10 снизу вверх, чекпоинты и мини‑кейсы).
- **Путь по уровням**: последовательные уровни (0–10), каждый из 3–5 уроков: видео → чат‑квиз → артефакт/действие.
- **Личные тренеры**: два бота — Лео (бизнес‑ментор) и Макс (трекер цели). Чаты хранятся, используется RAG, «память» и свёртки. Диалоги с Максом в чекпоинтах L4/L7 встроены в экран (embedded) и начинаютcя с серии коротких сообщений: напоминание цели/дедлайна и комментарий по регулярности (по журналу применений), затем — напоминание про квадрант B и ежедневный стресс‑менеджмент, после — вопрос о сложностях.
- **Мини-тур**: мягко познакомить пользователя с ключевыми точками BizLevel: Главная (Main Street), Цель (версии v1–v4, «что дальше»), Башня (переходы на уровни/чекпоинты/кейсы).
- **Библиотека**: список беслпатных полезных онлайн-курсов в сети, программы акселераций, а также списки грантов и субсидий для бизнеса
- **Цель и применение**: единая «Моя цель» (user_goal) и «Журнал применений» (practice_log). Экран цели включает компактную карточку цели (текст/прогресс/дни до дедлайна), модуль «Журнал применений» с быстрыми «Топ‑3» навыками, выпадающим списком «Другие навыки», индикатором «+1 день движения» после сохранения и заметной кнопкой «Вся история». В правом верхнем углу журнала — колокольчик для настройки напоминаний (общий экран, см. ниже). Чекпоинты L1/L4/L7 встроены в «Башню» и обновляют `user_goal`.
- **Навыки и прогресс**: 5 базовых навыков; за уровни начисляются очки навыков, в профиле отображается дерево навыков и артефакты.
- **GP‑экономика**: кошелёк GP, списание 1 GP за сообщение в чатах, открытие этажей за GP, бонусы и магазин пакетов.
- **Кроссплатформа**: iOS/Android/Web/Desktop; адаптивный UI, офлайн‑кеш данных, устойчивое видео.
---
## Пользовательский путь
### 0. Первый шаг (Уровень 0)
- Экран‑уровень: Intro → короткое видео → форма профиля (имя, аватар, цель, о себе) → финал.
- Завершение уровня 0 открывает «Этаж 1» в «Башне» и переводит пользователя к Уровню 1.
- Профиль используется для персонализации Лео/Макса и контекста RAG.
### Башня BizLevel (точка входа в уровни)
- Вертикальная карта: снизу «Уровень 0», выше «Этаж 1» и уровни 1–10. Узлы: level, mini_case, goal_checkpoint.
- Текущий уровень отображается раскрытой карточкой, остальные — компактные синие квадраты с иконками статуса (✓/🔒/★ для платных узлов этажа).
- Между уровнями вставлены узлы: мини‑кейсы (после 3/6/9) и чекпоинты цели (после 4/7/10 для v2/v3/v4). Открытие следующего узла требует завершения предыдущего.
- Навигация уровня — только через «Башню». Автоскролл к актуальному узлу и безопасные сообщения об ограничениях.
### Доступ и GP
- Бесплатно: уровни 0–3. Этажи с уровнями >3 открываются за GP (по умолчанию 1000 GP за этаж).
- Сообщения Лео/Максу стоят 1 GP; мини‑кейсы и чат‑квизы не списывают GP.
- Магазин GP (Web): пакеты 300/1400/3000 GP; бонусы: signup, заполнение профиля, серии действий.
### Уровни (уроки и чат‑квиз)
- Видео 9:16 с Vimeo (подписанные ссылки), корректная работа на Web/iOS/Android, кэш, фолбэки.
- Чат‑квиз: визуально как диалог с Лео, варианты ответов как «сообщения» пользователя, короткий фидбек, верный ответ разблокирует «Далее».
- Артефакты: описание и скачивание по подписанным URL (уровни 2–10).
### Цель и чекпоинты
- v1 создаётся на Уровне 1 (финальный блок «Набросок цели»).
- L4 «Фокус на регулярности»: экран — встроенный диалог с Максом (embedded). Приветствие: цель и дедлайн, обратная связь по регулярности (по данным `practice_log` за 7 дней), затем — напоминание о квадранте B/стресс‑менеджменте и вопрос о сложностях. Внизу — CTA «Завершить чекпоинт →» (возврат к цели/«Башне»).
- L7 «Система поддержки»: верх — встроенный диалог с Максом (о регулярности и системе напоминаний), низ — кнопки «Настроить напоминания» (общий экран выбора времени и дней недели) и «Завершить чекпоинт →».
- Страница «Цель» упрощена: компактная карточка + «Журнал применений» (без legacy недельных форм/версий).
### База тренеров (экраны чатов)
- Две карточки выбора бота: Лео (разбор уроков/бизнес) и Макс (цели/спринты). Список диалогов хранит только чаты с сообщениями.
- Чат использует персональный контекст: профиль, актуальные версии цели/спринтов, «память» и RAG по материалам.
- Стоимость сообщения — 1 GP; оффлайн/ошибки обрабатываются дружелюбно, без утечки PII.

### Напоминания (унифицировано)
- Локальные уведомления ОС. Единый экран настройки (время + дни недели) доступен из: чекпоинт L7, колокольчик в «Журнале применений», Профиль → Уведомления.
- Выбор сохраняется локально (Hive) и подтягивается как префилл. Значения по умолчанию — Пн/Ср/Пт 19:00. Все сценарии используют одну и ту же реализацию `NotificationsService`.
---
## Сервисная логика и архитектура
### Данные и бэкенд
- Supabase (Auth, PostgREST, Storage, Edge Functions). Все таблицы под RLS.
- Основные таблицы: `levels`, `lessons`, `user_progress`, `skills`, `user_skills`, `leo_chats`, `leo_messages`, `core_goals`, `weekly_progress`, `reminder_checks`, `motivational_quotes`, `documents`, `gp_wallets`, `gp_ledger`, `gp_purchases`, `gp_bonus_rules`, `gp_bonus_grants`, `floor_access`.
- Ключевые функции/триггеры:
  - `update_current_level(level_id)` — завершение уровня, обновление текущего и начисление очков навыка (UPSERT `user_skills`).
  - Триггер памяти чатов: вызов `leo-memory` для извлечения фактов и свёрток после ответов ассистента.
### ИИ‑функции
- `leo-chat` (Edge): единая точка для Лео/Макса; встроенный RAG, режимы default/quiz/case, рекомендованные «chips» для быстрых ответов.
- `leo-memory` (Edge): извлечение «памяти» из сообщений, обновление свёрток чатов; cron‑режим и триггер на `leo_messages`.
### GP‑ядро (RPC)
- Серверные функции (SECURITY DEFINER): `gp_balance`, `gp_spend`, `gp_floor_unlock`, `gp_bonus_claim` с идемпотентностью (`idx_gp_ledger_idem`) и транзакциями `SERIALIZABLE`.
- RLS owner‑only, индексы производительности, сведение баланса с `gp_ledger`.
### Клиент (Flutter)
- Навигация GoRouter: `/home` (Main Street), `/tower`, `/levels/:id`, `/chat`, `/goal`, `/goal-checkpoint/:version`, `/case/:id`, `/gp-store`.
- Провайдеры и репозитории с Hive SWR‑кешем; офлайн‑чтение уровней/уроков/целей/цитат/GP.
- Видео: кроссплатформенные контроллеры и фолбэки; устойчивость к офлайн/таймаутам; Sentry.
---
## Детали «Башни BizLevel»
### Типы узлов и их состояния
- **level**: номер, название, принадлежность к навыку, статусы `isCurrent`/`isCompleted`/`isLocked`.
- **mini_case**: мини‑кейс после уровней 3/6/9; состояния `started/completed/skipped`. До `completed/skipped` следующий уровень закрыт.
- **goal_checkpoint**: чекпоинты цели v2 (после 4), v3 (после 7), v4 (после 10); открыт при выполнении предыдущего уровня; `isCompleted` по факту наличия версии в `core_goals`.
### Геометрия и навигация
- Вертикальная 3‑колоночная сетка (статичная), «манхэттен‑пути» между соседними узлами с плавными 90°.
- Цвет пути по статусу исходного узла: завершённый — зелёный; активный — основной; заблокирован — серый (пониженная непрозрачность).
- Единая точка входа в уровень — тап по узлу уровня в «Башне». Автоскролл к текущему/целевому узлу (включая `scrollTo` query).
### Гейтинг и ошибки
- Блокировка уровня: если не завершён предыдущий узел (уровень/кейс/чекпоинт) — SnackBar «Завершите предыдущий уровень/узел».
- Этажи >1 открываются за GP; при отсутствии доступа — модал «Стоимость: 1000 GP» → покупка/магазин.
- Ошибки загрузки «Башни» — дружелюбный экран с «Повторить» (инвалидация провайдера). Все ошибки логируются в Sentry без PII.
### Доступность и телеметрия
- `Semantics(label, button)` на интерактивах; минимальные размерности ≥ 48 px; отсутствие overflow.
- Breadcrumbs: `tower_opened`, `tower_level_tap_open`, `tower_level_tap_locked`, `tower_checkpoint_completed`, `tower_retry`, `tower_autoscroll_done`.
---
## Экономика GP — подробности
### Модель данных и инварианты
- `gp_wallets`: баланс, суммарно заработано/потрачено; единственный источник текущего баланса.
- `gp_ledger`: журнал операций (purchase/spend_message/spend_floor/bonus/refund/adjustment) с `idempotency_key` (уникальный индекс).
- `floor_access`: доступ к этажу (PK по `user_id, floor_number`).
- Инвариант: любые изменения баланса происходят только транзакционно через RPC‑функции.
### RPC‑функции (SECURITY DEFINER, SERIALIZABLE)
- `gp_balance()` → `{balance, total_earned, total_spent}`; если кошелька нет — значения 0/0/0.
- `gp_spend(p_type, p_amount, p_reference_id, p_idempotency_key)` → `{balance_after}`; валидация, запись в `gp_ledger`, пересчёт `gp_wallets`, запрет баланса < 0.
- `gp_floor_unlock(p_floor, p_idempotency_key)` → `{balance_after}`; идемпотентная разблокировка этажа с фиксированным списанием.
- `gp_bonus_claim(p_rule_key)` → `{balance_after}`; единоразовые бонусы через `gp_bonus_grants`.
### Ошибки и UX
- Основные коды: `gp_insufficient_balance`, `gp_invalid_package`, `gp_already_processed`.
- Сообщение в чатах: при `gp_insufficient_balance` — модал «Пополнить GP» (переход в магазин с пресетом 1400 GP).
### Магазин и бонусы
- Магазин (Web): пакеты 300/1400/3000 GP; сервер валидирует пакет и сумму.
- Бонусы: `signup_bonus` (+30), `profile_completed` (+50), `all_three_cases_completed` (+200), `level_completed` (+20 за каждый завершённый уровень). Идемпотентность по PK grants и `idempotency_key` в леджере.
### Кэширование/офлайн
- `gpBalanceProvider`: SWR + Hive, фоновые рефетчи без редиректов. Ошибки/401 не вызывают `refreshSession` на клиенте.
---
## ИИ‑тренеры и чаты — детали
### Роли и системные промпты
- **Лео**: бизнес‑ментор; краткие практические рекомендации по урокам, персонализация примеров, ссылки на артефакты/практики.
- **Макс**: трекер цели; помогает формулировать v1→v4, недельные планы/итоги, держит фокус и динамику.
### Потоки и режимы
- Создание чата — только после первого сообщения пользователя (нет пустых чатов).
- Режимы: `default` (история+RAG+память), `quiz` (короткий ответ без истории/RAG), `case` (временный аналог quiz для мини‑кейсов).
- «Чипы» быстрых ответов — опционально приходят с сервера; при отсутствии формируются локально по версии цели.
### Персонализация и RAG
- Источники контекста: профиль, `core_goals` (versions), `weekly_progress`, последние «памятки» пользователя, свёртки диалогов, документы по уровню/скиллу (через `match_documents` с `metadata_filter`).
- `leo-memory`: триггер на `leo_messages`, батч‑эмбеддинги, upsert в `user_memories`, обновление свёрток чата.
### Стоимость сообщений и устойчивость
- Каждое сообщение списывает 1 GP (кроме режимов quiz/case). Экспоненциальные ретраи сети, дружелюбные ошибки, запрет вывода JWT/PII в логи.
---
## Цель — структура и чекпоинты (обновлено)
### Структура
- v1 «Семя цели»: что хочу за 28 дней, почему сейчас, главное препятствие.
- Цель хранится в `user_goal` (текст, метрики, даты), история недель — legacy.
### Чекпоинты (новый формат)
- L4 «Фокус на регулярности»: экран — встроенный диалог (embedded) с Максом: напоминание цели/дедлайна, комментарий по регулярности (по `practice_log` за 7 дней), напоминание про квадрант B и ежедневный стресс‑менеджмент, вопрос о сложностях. CTA «Завершить чекпоинт →».
- L7 «Система поддержки»: сверху — встроенный диалог с Максом, снизу — «Настроить напоминания» (общий экран выбора времени и дней недели) и «Завершить чекпоинт →».
### Недельный путь (legacy)
- Блок weekly/v2–v4 заменён на простую цель + «Журнал применений» и диалоги L4/L7.
---
## Уровни — уроки, прогресс и чат‑квиз
### Видео и платформы
- Источники: Supabase Storage (подписанные ссылки), Vimeo (Web/iOS фолбэки через iframe/webview).
- Гарантированные пропорции 9:16, без переполнений; кэш, «Пропустить»‑fallback при недоступности.
### Прогресс и разблокировки
- Последовательность: Intro → Видео → Квиз → (Артефакт) → Следующий урок.
- Прогресс хранится локально (Hive/SharedPreferences) и учитывает частичный просмотр (10 сек) и правильные ответы в квизе.
- «Завершить уровень» — доступно при просмотре всех видео и прохождении всех квизов; вызов `completeLevel(levelId)` обновляет `user_progress` и `current_level` (RPC).
### Чат‑квиз
- Визуально как диалог с Лео: приветствие, вопрос, выбор варианта как «сообщение пользователя», короткий фидбек ассистента.
- Верный ответ разблокирует «Далее»; неверный — «Попробовать снова».
---
## Профиль — навыки и артефакты
### Навыки
- 5 прогресс‑баров; +1 очко по навыку уровня при завершении (через `update_current_level`).
- SWR‑кеш, офлайн‑просмотр.
### Артефакты и аватар
- Список артефактов из завершённых уровней; скачивание по подписанным URL.
- Выбор аватара из набора (7 шт.), хранится `avatar_id` в профиле.
---
## Навигация и диплинки
- GoRouter‑маршруты: `/home`, `/tower`, `/levels/:id`, `/chat`, `/goal`, `/goal-checkpoint/:version`, `/case/:id`, `/gp-store`.
- Deep‑link `bizlevel://levels/<id>` ведёт на экран уровня; `bizlevel://auth/confirm` → `/login?registered=true`.
- Для Web включена `PathUrlStrategy` (URL без `#`).
---
## Безопасность и наблюдаемость
- RLS на всех таблицах; owner‑only политики для пользовательских данных.
- Sentry SDK, breadcrumbs по ключевым событиям; запрет логирования PII/JWT; обработка истёкшего JWT с разлогином.
- Supabase Advisors (security/perf) без критичных замечаний; индексы на горячих путях (RAG/GP/прогресс).
---
## Нефункциональные требования
- Производительность: плавная прокрутка «Башни», отсутствие лишних перерисовок (`RepaintBoundary`), кэширование данных/изображений/видео.
- Устойчивость к офлайн и таймаутам (экспоненциальный retry, дружелюбные ошибки на UI).
- Качество: линтер без ошибок, покрытие ключевых сценариев тестами (уровни/чаты/цели/GP/«Башня»), CI‑проверки и Supabase advisors в pipeline.
---
## Глоссарий
- **RAG** — Retrieval Augmented Generation; поиск релевантных документов/фактов с последующей генерацией ответа.
- **Чекпоинт цели** — специальный узел «Башни» для заполнения следующей версии цели (v2/v3/v4).
- **Мини‑кейс** — короткая практическая ситуация между уровнями (после 3/6/9) с диалогом‑фасилитацией.
- **GP** — внутренняя валюта Growth Points для оплаты сообщений и открытия этажей.
---
## UX‑принципы
- Единая точка открытия уровней — «Башня»; автоскролл к текущему узлу.
- Чат‑квиз стилистически совпадает с диалогом; формирует привычку общения с тренером.
- Доступность: семантика для интерактивов, минимальные размеры, отсутствие overflow.
- Адаптивность: мобильные/десктоп брейк‑пойнты, гладкая прокрутка, без «дёрганий».
---
## Учебная методика
1. **PBS (problem‑based solving)** — решаем реальные бизнес‑задачи, каждый уровень даёт применимый результат.
2. **Микролёрнинг** — короткие уроки «на ходу» + чат‑квиз для удержания внимания.
3. **Геймификация** — «Башня», очки навыков, чекпоинты цели, мини‑кейсы, GP‑экономика.
---
## Артефакты (уровни 2–10)
- Бизнес‑тулкиты: шаблоны учёта, чек‑листы, фреймворки УТП/питча, карты действий, юридические чек‑листы — доступны после соответствующих уровней.
---
## Каталог первых 10 уровней и связь с навыками
> Номера и названия — рабочие; каждый уровень привязан к одному из 5 базовых навыков.
1. **Уровень 1: Стартовый рывок — осознание целей** (🧠 Фокус лидера) — v1 «Семя цели».
2. **Уровень 2: Экспресс‑стресс‑менеджмент** (🧠 Фокус лидера) — чек‑лист практик.
3. **Уровень 3: Управление приоритетами (Эйзенхауэр)** (🧠 Фокус лидера) — шаблон спринт‑плана.
4. **Уровень 4: Базовый учёт доходов/расходов** (💰 Денежный контроль) — v2 «Цифры и метрики».
5. **Уровень 5: Создание УТП** (🎯 Магнит клиентов) — конструктор УТП.
6. **Уровень 6: Elevator Pitch** (🎯 Магнит клиентов) — карточка питча.
7. **Уровень 7: Мини‑планирование на неделю (SMART)** (⚡ Система действий) — v3 «SMART‑план».
8. **Уровень 8: Блиц‑опрос клиентов** (🎯 Магнит клиентов) — гайд вопросов.
9. **Уровень 9: Юридический чек‑лист** (💰 Денежный контроль) — чек‑лист.
10. **Уровень 10: Моя карта ближайших действий** (⚡ Система действий) — v4 «Финальная формула».
---
## Этапы достижения
1. Заполнить профиль (Уровень 0) — включить персонализацию и открыть «Этаж 1».
2. Пройти 10 уровней с чат‑квизами и артефактами, создать v1→v4 в чекпоинтах.
3. Пройти 28‑дневный путь к цели (4 недели), вести недельный прогресс и чек‑ины.
4. Разблокировать новые этажи «Башни» за GP и продолжать рост.
---
## Метрики успеха
- Прогресс «Башни»: доля пользователей, завершивших 1/3/10 уровни; автоскролл‑сценарии без ошибок.
- Экономика GP: средний баланс, конверсия в «открытие этажа», списания 1 GP за сообщение, доля бонусов.
- Чаты: среднее число полезных диалогов, вовлечённость в чат‑квизы, использование recommended chips.
- Цели: доля пользователей с v1→v4, частота недельных чек‑инов, динамика по метрике.
- Навыки: прирост очков по 5 навыкам.
---
## Roadmap (черновик)
- Улучшение RAG (гибридный поиск, реранк), расширение «памяти» и свёрток.
- Уведомления: переход от локальных к FCM; календарные интеграции.
- GP‑Store: мобильные SDK, промо‑правила бонусов, витрина офферов.
- Башня: новые этажи, дополнительные мини‑кейсы, визуальные темы.
- Экспорт целей (PDF), публикация достижений, социальные механики.